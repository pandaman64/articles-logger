generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider     = "mysql"
  url          = env("DATABASE_URL")
  relationMode = "prisma"
}

model Article {
  id    Int     @id @default(autoincrement())
  title String?
  text  String?
  url   String?
}
